$NetBSD$

* Part of patchset to build on NetBSD

--- third_party/rust/chromium_crates_io/vendor/cxxbridge-macro-1.0.116/Cargo.toml.orig	2024-03-19 22:15:22.890505800 +0000
+++ third_party/rust/chromium_crates_io/vendor/cxxbridge-macro-1.0.116/Cargo.toml
@@ -1,41 +1,83 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2021"
+rust-version = "1.60"
 name = "cxxbridge-macro"
 version = "1.0.116"
 authors = ["David Tolnay <dtolnay@gmail.com>"]
-categories = ["development-tools::ffi"]
+exclude = [
+    "build.rs",
+    "README.md",
+]
 description = "Implementation detail of the `cxx` crate."
-edition = "2021"
-exclude = ["build.rs", "README.md"]
 homepage = "https://cxx.rs"
+readme = "README.md"
 keywords = ["ffi"]
+categories = ["development-tools::ffi"]
 license = "MIT OR Apache-2.0"
 repository = "https://github.com/dtolnay/cxx"
-rust-version = "1.60"
+
+[package.metadata.docs.rs]
+rustdoc-args = ["--generate-link-to-definition"]
+targets = ["x86_64-unknown-linux-gnu"]
 
 [lib]
 proc-macro = true
 
-[features]
-# incomplete features that are not covered by a compatibility guarantee:
-experimental-async-fn = []
-experimental-enum-variants-from-header = ["clang-ast", "flate2", "memmap", "serde", "serde_derive", "serde_json"]
+[dependencies.clang-ast]
+version = "0.1.18"
+optional = true
+
+[dependencies.flate2]
+version = "1.0.26"
+optional = true
+
+[dependencies.memmap]
+version = "0.7"
+optional = true
+
+[dependencies.proc-macro2]
+version = "1.0.74"
+
+[dependencies.quote]
+version = "1.0.35"
+
+[dependencies.serde]
+version = "1.0.166"
+optional = true
+
+[dependencies.serde_derive]
+version = "1.0.166"
+optional = true
+
+[dependencies.serde_json]
+version = "1.0.100"
+optional = true
+
+[dependencies.syn]
+version = "2.0.46"
+features = ["full"]
 
-[dependencies]
-proc-macro2 = "1.0.74"
-quote = "1.0.35"
-syn = { version = "2.0.46", features = ["full"] }
-
-# optional dependencies:
-clang-ast = { version = "0.1.18", optional = true }
-flate2 = { version = "1.0.26", optional = true }
-memmap = { version = "0.7", optional = true }
-serde = { version = "1.0.166", optional = true }
-serde_derive = { version = "1.0.166", optional = true }
-serde_json = { version = "1.0.100", optional = true }
+[dev-dependencies.cxx]
+version = "1.0"
 
-[dev-dependencies]
-cxx = { version = "1.0", path = ".." }
-
-[package.metadata.docs.rs]
-targets = ["x86_64-unknown-linux-gnu"]
-rustdoc-args = ["--generate-link-to-definition"]
+[features]
+experimental-async-fn = []
+experimental-enum-variants-from-header = [
+    "clang-ast",
+    "flate2",
+    "memmap",
+    "serde",
+    "serde_derive",
+    "serde_json",
+]
