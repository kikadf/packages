$NetBSD$

--- third_party/rust/chromium_crates_io/vendor/qr_code-2.0.0/Cargo.toml.orig	2024-03-06 00:15:17.297133400 +0000
+++ third_party/rust/chromium_crates_io/vendor/qr_code-2.0.0/Cargo.toml
@@ -1,31 +1,43 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
 name = "qr_code"
-description = "QR code encoder in Rust, support structured append (data in multiple qrcodes)"
-license = "MIT OR Apache-2.0"
 version = "2.0.0"
-edition = "2018"
-authors = ["kennytm <kennytm@gmail.com>", "Riccardo Casatta <riccardo.casatta@gmail.com>"]
-keywords = ["qrcode"]
-repository = "https://github.com/RCasatta/qr_code"
-readme = "README.md"
-documentation = "http://docs.rs/qr_code"
+authors = [
+    "kennytm <kennytm@gmail.com>",
+    "Riccardo Casatta <riccardo.casatta@gmail.com>",
+]
 exclude = [
-    ".github", ".gitignore", ".idea"
+    ".github",
+    ".gitignore",
+    ".idea",
 ]
+description = "QR code encoder in Rust, support structured append (data in multiple qrcodes)"
+documentation = "http://docs.rs/qr_code"
+readme = "README.md"
+keywords = ["qrcode"]
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/RCasatta/qr_code"
 
-[dependencies]
-bmp-monochrome = { version = "1.0.0", default-features = false, optional = true }
-g2p = { version = "0.4", default-features = false, optional = true }
-arbitrary = { version = "0.4.7", default-features = false, optional = true, features = ["derive"]}
-
-[dev-dependencies]
-rand = "0.7.3"
-hex = "0.4.2"
-
-[features]
-bmp = ["bmp-monochrome"]
-decode = ["g2p"]
-fuzz = ["arbitrary"]
+[package.metadata.docs.rs]
+features = [
+    "bmp",
+    "decode",
+]
+rustdoc-args = [
+    "--cfg",
+    "docsrs",
+]
 
 [[example]]
 name = "encode_string"
@@ -36,8 +48,34 @@ required-features = ["bmp"]
 
 [[example]]
 name = "decode_bmp"
-required-features = ["bmp", "decode"]
+required-features = [
+    "bmp",
+    "decode",
+]
 
-[package.metadata.docs.rs]
-features = ["bmp", "decode"]
-rustdoc-args = ["--cfg", "docsrs"]
+[dependencies.arbitrary]
+version = "0.4.7"
+features = ["derive"]
+optional = true
+default-features = false
+
+[dependencies.bmp-monochrome]
+version = "1.0.0"
+optional = true
+default-features = false
+
+[dependencies.g2p]
+version = "0.4"
+optional = true
+default-features = false
+
+[dev-dependencies.hex]
+version = "0.4.2"
+
+[dev-dependencies.rand]
+version = "0.7.3"
+
+[features]
+bmp = ["bmp-monochrome"]
+decode = ["g2p"]
+fuzz = ["arbitrary"]
