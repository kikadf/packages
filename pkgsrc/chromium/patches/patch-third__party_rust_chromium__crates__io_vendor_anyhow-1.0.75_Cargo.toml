$NetBSD$

--- third_party/rust/chromium_crates_io/vendor/anyhow-1.0.75/Cargo.toml.orig	2024-03-06 00:15:17.165121800 +0000
+++ third_party/rust/chromium_crates_io/vendor/anyhow-1.0.75/Cargo.toml
@@ -1,33 +1,67 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
+rust-version = "1.39"
 name = "anyhow"
-version = "1.0.75" # remember to update html_root_url
+version = "1.0.75"
 authors = ["David Tolnay <dtolnay@gmail.com>"]
-categories = ["rust-patterns", "no-std"]
 description = "Flexible concrete Error type built on std::error::Error"
 documentation = "https://docs.rs/anyhow"
-edition = "2018"
-keywords = ["error", "error-handling"]
+readme = "README.md"
+keywords = [
+    "error",
+    "error-handling",
+]
+categories = [
+    "rust-patterns",
+    "no-std",
+]
 license = "MIT OR Apache-2.0"
 repository = "https://github.com/dtolnay/anyhow"
-rust-version = "1.39"
-
-[features]
-default = ["std"]
-std = []
 
-[dependencies]
-backtrace = { version = "0.3.51", optional = true }
-
-[dev-dependencies]
-futures = { version = "0.3", default-features = false }
-rustversion = "1.0.6"
-syn = { version = "2.0", features = ["full"] }
-thiserror = "1.0.45"
-trybuild = { version = "1.0.66", features = ["diff"] }
+[package.metadata.docs.rs]
+rustdoc-args = [
+    "--cfg",
+    "doc_cfg",
+    "--generate-link-to-definition",
+]
+targets = ["x86_64-unknown-linux-gnu"]
 
 [lib]
 doc-scrape-examples = false
 
-[package.metadata.docs.rs]
-targets = ["x86_64-unknown-linux-gnu"]
-rustdoc-args = ["--cfg", "doc_cfg", "--generate-link-to-definition"]
+[dependencies.backtrace]
+version = "0.3.51"
+optional = true
+
+[dev-dependencies.futures]
+version = "0.3"
+default-features = false
+
+[dev-dependencies.rustversion]
+version = "1.0.6"
+
+[dev-dependencies.syn]
+version = "2.0"
+features = ["full"]
+
+[dev-dependencies.thiserror]
+version = "1.0.45"
+
+[dev-dependencies.trybuild]
+version = "1.0.66"
+features = ["diff"]
+
+[features]
+default = ["std"]
+std = []
