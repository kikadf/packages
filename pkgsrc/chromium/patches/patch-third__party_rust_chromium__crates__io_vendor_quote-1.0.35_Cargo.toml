$NetBSD$

* Part of patchset to build on NetBSD

--- third_party/rust/chromium_crates_io/vendor/quote-1.0.35/Cargo.toml.orig	2024-03-19 22:15:22.966512700 +0000
+++ third_party/rust/chromium_crates_io/vendor/quote-1.0.35/Cargo.toml
@@ -1,36 +1,50 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
+rust-version = "1.56"
 name = "quote"
-version = "1.0.35" # don't forget to update html_root_url, version in readme for breaking changes
+version = "1.0.35"
 authors = ["David Tolnay <dtolnay@gmail.com>"]
 autobenches = false
-categories = ["development-tools::procedural-macro-helpers"]
 description = "Quasi-quoting macro quote!(...)"
 documentation = "https://docs.rs/quote/"
-edition = "2018"
-keywords = ["macros", "syn"]
+readme = "README.md"
+keywords = [
+    "macros",
+    "syn",
+]
+categories = ["development-tools::procedural-macro-helpers"]
 license = "MIT OR Apache-2.0"
 repository = "https://github.com/dtolnay/quote"
-rust-version = "1.56"
-
-[dependencies]
-proc-macro2 = { version = "1.0.74", default-features = false }
-
-[dev-dependencies]
-rustversion = "1.0"
-trybuild = { version = "1.0.66", features = ["diff"] }
 
-[features]
-default = ["proc-macro"]
-# Disabling the proc-macro feature removes the dynamic library dependency on
-# libproc_macro in the rustc compiler.
-proc-macro = ["proc-macro2/proc-macro"]
+[package.metadata.docs.rs]
+rustdoc-args = ["--generate-link-to-definition"]
+targets = ["x86_64-unknown-linux-gnu"]
 
 [lib]
 doc-scrape-examples = false
 
-[workspace]
-members = ["benches"]
+[dependencies.proc-macro2]
+version = "1.0.74"
+default-features = false
+
+[dev-dependencies.rustversion]
+version = "1.0"
+
+[dev-dependencies.trybuild]
+version = "1.0.66"
+features = ["diff"]
 
-[package.metadata.docs.rs]
-targets = ["x86_64-unknown-linux-gnu"]
-rustdoc-args = ["--generate-link-to-definition"]
+[features]
+default = ["proc-macro"]
+proc-macro = ["proc-macro2/proc-macro"]
