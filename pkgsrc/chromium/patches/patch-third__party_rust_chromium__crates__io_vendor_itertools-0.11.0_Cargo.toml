$NetBSD$

* Part of patchset to build on NetBSD

--- third_party/rust/chromium_crates_io/vendor/itertools-0.11.0/Cargo.toml.orig	2024-03-06 00:15:17.237128000 +0000
+++ third_party/rust/chromium_crates_io/vendor/itertools-0.11.0/Cargo.toml
@@ -1,44 +1,44 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
+rust-version = "1.36.0"
 name = "itertools"
 version = "0.11.0"
-
-license = "MIT OR Apache-2.0"
-repository = "https://github.com/rust-itertools/itertools"
-documentation = "https://docs.rs/itertools/"
 authors = ["bluss"]
-readme = "README.md"
-
-description = "Extra iterator adaptors, iterator methods, free functions, and macros."
-
-keywords = ["iterator", "data-structure", "zip", "product", "group-by"]
-categories = ["algorithms", "rust-patterns"]
 exclude = ["/bors.toml"]
+description = "Extra iterator adaptors, iterator methods, free functions, and macros."
+documentation = "https://docs.rs/itertools/"
+readme = "README.md"
+keywords = [
+    "iterator",
+    "data-structure",
+    "zip",
+    "product",
+    "group-by",
+]
+categories = [
+    "algorithms",
+    "rust-patterns",
+]
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/rust-itertools/itertools"
 
-edition = "2018"
-
-rust-version = "1.36.0"
+[profile.bench]
+debug = 2
 
 [lib]
-bench = false
 test = false
-
-[dependencies]
-either = { version = "1.0", default-features = false }
-
-[dev-dependencies]
-rand = "0.7"
-criterion = "0.4.0"
-paste = "1.0.0"  # Used in test_std to instantiate generic tests
-permutohedron = "0.2"
-quickcheck = { version = "0.9", default_features = false }
-
-[features]
-default = ["use_std"]
-use_std = ["use_alloc", "either/use_std"]
-use_alloc = []
-
-[profile]
-bench = { debug = true }
+bench = false
 
 [[bench]]
 name = "tuple_combinations"
@@ -71,3 +71,31 @@ harness = false
 [[bench]]
 name = "powerset"
 harness = false
+
+[dependencies.either]
+version = "1.0"
+default-features = false
+
+[dev-dependencies.criterion]
+version = "0.4.0"
+
+[dev-dependencies.paste]
+version = "1.0.0"
+
+[dev-dependencies.permutohedron]
+version = "0.2"
+
+[dev-dependencies.quickcheck]
+version = "0.9"
+default_features = false
+
+[dev-dependencies.rand]
+version = "0.7"
+
+[features]
+default = ["use_std"]
+use_alloc = []
+use_std = [
+    "use_alloc",
+    "either/use_std",
+]
