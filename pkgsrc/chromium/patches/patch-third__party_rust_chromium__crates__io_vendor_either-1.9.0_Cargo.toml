$NetBSD$

--- third_party/rust/chromium_crates_io/vendor/either-1.9.0/Cargo.toml.orig	2024-03-06 00:15:17.225127000 +0000
+++ third_party/rust/chromium_crates_io/vendor/either-1.9.0/Cargo.toml
@@ -1,35 +1,40 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies
+#
+# If you believe there's an error in this file please file an
+# issue against the rust-lang/cargo repository. If you're
+# editing this file be aware that the upstream Cargo.toml
+# will likely look very different (and much more reasonable)
+
 [package]
+edition = "2018"
 name = "either"
 version = "1.9.0"
 authors = ["bluss"]
-edition = "2018"
-rust-version = "1.36"
-
-license = "MIT OR Apache-2.0"
-repository = "https://github.com/bluss/either"
+description = "The enum `Either` with variants `Left` and `Right` is a general purpose sum type with two cases.\n"
 documentation = "https://docs.rs/either/1/"
 readme = "README-crates.io.md"
-
-description = """
-The enum `Either` with variants `Left` and `Right` is a general purpose sum type with two cases.
-"""
-
 keywords = ["data-structure", "no_std"]
 categories = ["data-structures", "no-std"]
-
-[dependencies]
-serde = { version = "1.0", optional = true, features = ["derive"] }
-
-[features]
-default = ["use_std"]
-use_std = []
-
-[dev-dependencies]
-serde_json = "1.0.0"
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/bluss/either"
+[package.metadata.docs.rs]
+features = ["serde"]
 
 [package.metadata.release]
 no-dev-version = true
 tag-name = "{{version}}"
+[dependencies.serde]
+version = "1.0"
+features = ["derive"]
+optional = true
+[dev-dependencies.serde_json]
+version = "1.0.0"
 
-[package.metadata.docs.rs]
-features = ["serde"]
+[features]
+default = ["use_std"]
+use_std = []
