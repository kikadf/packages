$NetBSD$

* Part of patchset to build on NetBSD

--- third_party/rust/chromium_crates_io/vendor/skrifa-0.15.2/Cargo.toml.orig	2024-03-06 00:15:17.401142400 +0000
+++ third_party/rust/chromium_crates_io/vendor/skrifa-0.15.2/Cargo.toml
@@ -1,21 +1,40 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2021"
 name = "skrifa"
 version = "0.15.2"
-edition = "2021"
-license = "MIT/Apache-2.0"
 description = "Metadata reader and glyph scaler for OpenType fonts."
-repository = "https://github.com/googlefonts/fontations"
 readme = "README.md"
-categories = ["text-processing", "parsing", "graphics"]
+categories = [
+    "text-processing",
+    "parsing",
+    "graphics",
+]
+license = "MIT/Apache-2.0"
+repository = "https://github.com/googlefonts/fontations"
 
-[dependencies]
-read-fonts = { version = "0.15.1", path = "../read-fonts" }
+[dependencies.read-fonts]
+version = "0.15.1"
 
-[dev-dependencies]
-font-test-data = { path = "../font-test-data" }
-read-fonts = { version = "0.15.1", path = "../read-fonts", features = [
+[dev-dependencies.read-fonts]
+version = "0.15.1"
+features = [
     "scaler_test",
     "serde",
-] }
-serde = "1.0"
-serde_json = "1.0"
+]
+
+[dev-dependencies.serde]
+version = "1.0"
+
+[dev-dependencies.serde_json]
+version = "1.0"
