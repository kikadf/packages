$NetBSD$

* Part of patchset to build on NetBSD

--- third_party/rust/chromium_crates_io/vendor/bitflags-2.4.2/Cargo.toml.orig	2024-03-19 22:15:22.834500800 +0000
+++ third_party/rust/chromium_crates_io/vendor/bitflags-2.4.2/Cargo.toml
@@ -1,43 +1,95 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
-name = "bitflags"
-# NB: When modifying, also modify the number in readme (for breaking changes)
-version = "2.4.2"
 edition = "2021"
 rust-version = "1.56.0"
+name = "bitflags"
+version = "2.4.2"
 authors = ["The Rust Project Developers"]
-license = "MIT OR Apache-2.0"
-keywords = ["bit", "bitmask", "bitflags", "flags"]
-readme = "README.md"
-repository = "https://github.com/bitflags/bitflags"
-homepage = "https://github.com/bitflags/bitflags"
-documentation = "https://docs.rs/bitflags"
-categories = ["no-std"]
+exclude = [
+    "/tests",
+    "/.github",
+]
 description = """
 A macro to generate structures which behave like bitflags.
 """
-exclude = ["/tests", "/.github"]
+homepage = "https://github.com/bitflags/bitflags"
+documentation = "https://docs.rs/bitflags"
+readme = "README.md"
+keywords = [
+    "bit",
+    "bitmask",
+    "bitflags",
+    "flags",
+]
+categories = ["no-std"]
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/bitflags/bitflags"
 
-[dependencies]
-serde = { version = "1.0", optional = true, default-features = false }
-arbitrary = { version = "1.0", optional = true }
-bytemuck = { version = "1.0", optional = true }
-core = { version = "1.0.0", optional = true, package = "rustc-std-workspace-core" }
-compiler_builtins = { version = "0.1.2", optional = true }
-
-[dev-dependencies]
-trybuild = "1.0"
-rustversion = "1.0"
-serde_derive = "1.0"
-serde_json = "1.0"
-serde_test = "1.0"
-zerocopy = "0.6"
-arbitrary = { version = "1.0", features = ["derive"] }
-bytemuck = { version = "1.0", features = ["derive"] }
+[package.metadata.docs.rs]
+features = ["example_generated"]
+
+[dependencies.arbitrary]
+version = "1.0"
+optional = true
+
+[dependencies.bytemuck]
+version = "1.0"
+optional = true
+
+[dependencies.compiler_builtins]
+version = "0.1.2"
+optional = true
+
+[dependencies.core]
+version = "1.0.0"
+optional = true
+package = "rustc-std-workspace-core"
+
+[dependencies.serde]
+version = "1.0"
+optional = true
+default-features = false
+
+[dev-dependencies.arbitrary]
+version = "1.0"
+features = ["derive"]
+
+[dev-dependencies.bytemuck]
+version = "1.0"
+features = ["derive"]
+
+[dev-dependencies.rustversion]
+version = "1.0"
+
+[dev-dependencies.serde_derive]
+version = "1.0"
+
+[dev-dependencies.serde_json]
+version = "1.0"
+
+[dev-dependencies.serde_test]
+version = "1.0"
+
+[dev-dependencies.trybuild]
+version = "1.0"
+
+[dev-dependencies.zerocopy]
+version = "0.6"
 
 [features]
-std = []
 example_generated = []
-rustc-dep-of-std = ["core", "compiler_builtins"]
-
-[package.metadata.docs.rs]
-features = ["example_generated"]
+rustc-dep-of-std = [
+    "core",
+    "compiler_builtins",
+]
+std = []
