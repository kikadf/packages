$NetBSD$

--- third_party/rust/chromium_crates_io/vendor/itoa-1.0.9/Cargo.toml.orig	2024-03-06 00:15:17.241128400 +0000
+++ third_party/rust/chromium_crates_io/vendor/itoa-1.0.9/Cargo.toml
@@ -1,23 +1,43 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
+edition = "2018"
+rust-version = "1.36"
 name = "itoa"
-version = "1.0.9" # remember to update html_root_url
+version = "1.0.9"
 authors = ["David Tolnay <dtolnay@gmail.com>"]
-categories = ["value-formatting", "no-std", "no-std::no-alloc"]
+exclude = [
+    "performance.png",
+    "chart/**",
+]
 description = "Fast integer primitive to string conversion"
 documentation = "https://docs.rs/itoa"
-edition = "2018"
-exclude = ["performance.png", "chart/**"]
+readme = "README.md"
 keywords = ["integer"]
+categories = [
+    "value-formatting",
+    "no-std",
+    "no-std::no-alloc",
+]
 license = "MIT OR Apache-2.0"
 repository = "https://github.com/dtolnay/itoa"
-rust-version = "1.36"
 
-[dependencies]
-no-panic = { version = "0.1", optional = true }
+[package.metadata.docs.rs]
+rustdoc-args = ["--generate-link-to-definition"]
+targets = ["x86_64-unknown-linux-gnu"]
 
 [lib]
 doc-scrape-examples = false
 
-[package.metadata.docs.rs]
-targets = ["x86_64-unknown-linux-gnu"]
-rustdoc-args = ["--generate-link-to-definition"]
+[dependencies.no-panic]
+version = "0.1"
+optional = true
