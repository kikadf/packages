$NetBSD$

* Part of patchset to build on NetBSD

--- third_party/rust/chromium_crates_io/vendor/libc-0.2.150/Cargo.toml.orig	2024-03-06 00:15:17.245128900 +0000
+++ third_party/rust/chromium_crates_io/vendor/libc-0.2.150/Cargo.toml
@@ -1,23 +1,53 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
 name = "libc"
 version = "0.2.150"
 authors = ["The Rust Project Developers"]
-license = "MIT OR Apache-2.0"
-readme = "README.md"
-repository = "https://github.com/rust-lang/libc"
-homepage = "https://github.com/rust-lang/libc"
-documentation = "https://docs.rs/libc/"
-keywords = ["libc", "ffi", "bindings", "operating", "system"]
-categories = ["external-ffi-bindings", "no-std", "os"]
 build = "build.rs"
-exclude = ["/ci/*", "/.github/*", "/.cirrus.yml", "/triagebot.toml"]
+exclude = [
+    "/ci/*",
+    "/.github/*",
+    "/.cirrus.yml",
+    "/triagebot.toml",
+]
 description = """
 Raw FFI bindings to platform libraries like libc.
 """
+homepage = "https://github.com/rust-lang/libc"
+documentation = "https://docs.rs/libc/"
+readme = "README.md"
+keywords = [
+    "libc",
+    "ffi",
+    "bindings",
+    "operating",
+    "system",
+]
+categories = [
+    "external-ffi-bindings",
+    "no-std",
+    "os",
+]
+license = "MIT OR Apache-2.0"
+repository = "https://github.com/rust-lang/libc"
 
 [package.metadata.docs.rs]
-features = ["const-extern-fn", "extra_traits"]
+cargo-args = ["-Zbuild-std=core"]
 default-target = "x86_64-unknown-linux-gnu"
+features = [
+    "const-extern-fn",
+    "extra_traits",
+]
 targets = [
     "aarch64-apple-ios",
     "aarch64-linux-android",
@@ -124,22 +154,21 @@ targets = [
     "x86_64-unknown-netbsd",
     "x86_64-unknown-openbsd",
     "x86_64-unknown-redox",
-    "x86_64-wrs-vxworks"
+    "x86_64-wrs-vxworks",
 ]
-cargo-args = ["-Zbuild-std=core"]
 
-[dependencies]
-rustc-std-workspace-core = { version = "1.0.0", optional = true }
+[dependencies.rustc-std-workspace-core]
+version = "1.0.0"
+optional = true
 
 [features]
-default = ["std"]
-std = []
 align = []
-rustc-dep-of-std = ['align', 'rustc-std-workspace-core']
-extra_traits = []
 const-extern-fn = []
-# use_std is deprecated, use `std` instead
-use_std = ['std']
-
-[workspace]
-members = ["libc-test"]
+default = ["std"]
+extra_traits = []
+rustc-dep-of-std = [
+    "align",
+    "rustc-std-workspace-core",
+]
+std = []
+use_std = ["std"]
