$NetBSD$

--- third_party/rust/chromium_crates_io/vendor/rustc-demangle-0.1.23/Cargo.toml.orig	2024-03-06 00:15:17.373139900 +0000
+++ third_party/rust/chromium_crates_io/vendor/rustc-demangle-0.1.23/Cargo.toml
@@ -1,30 +1,49 @@
+# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
+#
+# When uploading crates to the registry Cargo will automatically
+# "normalize" Cargo.toml files for maximal compatibility
+# with all versions of Cargo and also rewrite `path` dependencies
+# to registry (e.g., crates.io) dependencies.
+#
+# If you are reading this file be aware that the original Cargo.toml
+# will likely look very different (and much more reasonable).
+# See Cargo.toml.orig for the original contents.
+
 [package]
 name = "rustc-demangle"
 version = "0.1.23"
 authors = ["Alex Crichton <alex@alexcrichton.com>"]
-license = "MIT/Apache-2.0"
-readme = "README.md"
-repository = "https://github.com/alexcrichton/rustc-demangle"
-homepage = "https://github.com/alexcrichton/rustc-demangle"
-documentation = "https://docs.rs/rustc-demangle"
 description = """
 Rust compiler symbol demangling.
 """
+homepage = "https://github.com/alexcrichton/rustc-demangle"
+documentation = "https://docs.rs/rustc-demangle"
+readme = "README.md"
+license = "MIT/Apache-2.0"
+repository = "https://github.com/alexcrichton/rustc-demangle"
 
-[workspace]
-members = ["crates/capi", "fuzz"]
-
-[dependencies]
-core = { version = '1.0.0', optional = true, package = 'rustc-std-workspace-core' }
-compiler_builtins = { version = '0.1.2', optional = true }
-
-[features]
-rustc-dep-of-std = ['core', 'compiler_builtins']
-std = []
+[package.metadata.docs.rs]
+features = ["std"]
+rustdoc-args = [
+    "--cfg",
+    "docsrs",
+]
 
 [profile.release]
 lto = true
 
-[package.metadata.docs.rs]
-features = ["std"]
-rustdoc-args = ["--cfg", "docsrs"]
+[dependencies.compiler_builtins]
+version = "0.1.2"
+optional = true
+
+[dependencies.core]
+version = "1.0.0"
+optional = true
+package = "rustc-std-workspace-core"
+
+[features]
+rustc-dep-of-std = [
+    "core",
+    "compiler_builtins",
+]
+std = []
